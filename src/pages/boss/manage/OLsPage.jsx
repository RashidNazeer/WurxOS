import UserListPage from './UserListPage';
export default function OLsPage() {
  return <UserListPage role="ol" title="Operation Leads" emptyHint="Add an Operation Lead to coordinate teams and brands." />;
}
