import UserListPage from './UserListPage';
export default function DevelopersPage() {
  return <UserListPage role="developer" title="Developers" emptyHint="Add engineers who need access to maintain the platform." />;
}
