/**
 * v1 verbatim-port shim — v1 components import RichTextEditor from
 * `../shared/RichTextEditor`. v2's lives at `../common/RichTextEditor`.
 * This module is just a path-alias re-export so v1 markup compiles
 * without modification.
 */
export { default } from '../common/RichTextEditor';
